# Marketing site (Vite) — copy to .env and fill in values
# Use the same Supabase project as platform/.env.local

VITE_PLATFORM_URL=http://localhost:3001

# From Supabase Dashboard → Settings → API (same as platform NEXT_PUBLIC_* keys)
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
