import { AdminDashboardPreview } from '@/components/admin/AdminDashboardPreview';

/** Public demo — admin panel (no login required). */
export default function AdminPreviewPage() {
  return <AdminDashboardPreview />;
}
